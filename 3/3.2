"Скласти програму для графічного зображення подільності чисел від 1 до n (значення n вводиться з клавіатури). У кожному рядку треба надрукувати чергове число і стільки символів +, скільки є дільників у цього числа. Виконав Шевчук Владислав"
def plot_divisibility_sequence(n):
    for num in range(1, n + 1):
        divisors = [i for i in range(1, num + 1) if num % i == 0]
        print(f"{num}: {'+' * len(divisors)}")

# Введення значення n з клавіатури
n = int(input("Введіть значення n: "))

# Виклик функції для графічного зображення подільності чисел
plot_divisibility_sequence(n)
